'use strict';

describe('wptApp.environment module', function() {

  beforeEach(module('wptApp.environment'));

  describe('environment controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var environameController = $controller('EnvironmentController');
      expect(environameController).toBeDefined();
    }));

  });
});